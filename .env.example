# ===========================================
# Kisan-AGI Backend Environment Configuration
# ===========================================
# Copy this file to `.env` for local development
# For Vercel: Add these in Dashboard > Settings > Environment Variables
# ===========================================

# --- MongoDB Configuration ---
# Your MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# --- Google Cloud Configuration ---
# The GCloud Project ID (REQUIRED - no auto-inference in serverless)
# Use either GCLOUD_PROJECT or PROJECT_ID (both are checked)
GCLOUD_PROJECT=your-gcloud-project-id
PROJECT_ID=your-gcloud-project-id

# The GCloud region for Vertex AI (defaults to us-central1 if not set)
GCLOUD_LOCATION=us-central1

# --- Google Service Account (REQUIRED for Vercel) ---
# For LOCAL development: Place `service-account.json` in the root directory
# For VERCEL: Paste the entire JSON content as a single-line string below
# 
# To convert your service-account.json to a single line:
#   1. Open your service-account.json
#   2. Run: cat service-account.json | jq -c .
#   3. Copy the output (single line JSON)
#   4. Paste it as the value for SERVICE_ACCOUNT_JSON in Vercel Dashboard
#
SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"..."}

# --- Google Maps API Key ---
# Required for the dealer search functionality
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# --- Optional Configuration ---
# Server port (only used for local development)
PORT=5000

# Node environment (development/production)
NODE_ENV=development
